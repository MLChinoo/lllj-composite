<Window x:Class="atri_composite.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="正在索引文件..." Height="200" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA"
        ResizeMode="NoResize"
        WindowStyle="ThreeDBorderWindow"
        Icon="icon.ico">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Header -->
        <TextBlock x:Name="txtTitle" Grid.Row="0" Text="正在建立文件索引..." FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>

        <!-- Progress Bar -->
        <ProgressBar x:Name="progressBar" Grid.Row="1" Height="20" Minimum="0" Maximum="100" 
                     Background="#ECF0F1" Foreground="#2ECC71" BorderThickness="0"/>

        <!-- Info Grid -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Left Details -->
            <TextBlock x:Name="txtProgress" Grid.Row="0" Grid.Column="0" Text="已扫描: 0/0 文件夹" FontSize="12" Foreground="#7F8C8D"/>
            <TextBlock x:Name="txtIndexed" Grid.Row="1" Grid.Column="0" Text="已索引: 0 个文件 (.stand, .sinfo, .txt, .pbd, .png, .tlg)" FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0"/>

            <!-- Right Memory -->
            <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                <TextBlock Text="索引占用内存" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Right"/>
                <TextBlock x:Name="txtMemory" Text="0.00 KB" FontSize="20" FontWeight="Bold" Foreground="#34495E" HorizontalAlignment="Right" Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
